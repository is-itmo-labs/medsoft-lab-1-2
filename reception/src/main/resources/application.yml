server:
  port: 8080
spring:
  datasource:
  # Reception does not use DB in first iteration (DB is used only by core), but we configure minimal
  jpa:
    hibernate:
      ddl-auto: none
logging:
  logger:
    hl7: INFO

reception:
  core-fhir-url: http://localhost:8081/fhir